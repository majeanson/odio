{
  "featureKey": "odio-2026-012",
  "title": "Versioning — Version Chain, Waveform Editor, Waveform Player, Freeze",
  "status": "active",
  "priority": 2,
  "buildOrder": 10,
  "schemaVersion": 1,
  "domain": "versioning",
  "tags": [
    "versioning",
    "version-history",
    "freeze",
    "wavesurfer",
    "waveform-editor",
    "waveform-player",
    "localstorage",
    "skip-cuts",
    "split"
  ],
  "problem": "A raw recording needs to evolve from v1 (messy, includes setup and dead air) to vFinal (clean, trimmed) through collaborative edits — with full history and a clear end state (freeze). The band needs to listen to any version, compare versions, and hear exactly what a cut sounds like before committing.",
  "analysis": "Two dedicated components with separate concerns:\n\n**WaveformPlayer** (clip detail page) — pure playback. Receives activeCuts from parent (ClipDetailClient). Visualises cuts as blue non-draggable regions (pointerEvents: none on regions so they don't block click-to-seek). Skip-cuts during playback via timeupdate handler. H:MM:SS clock + virtual duration display. Stamp chips are tappable — each seeks to that timestamp. Big 64px play button. finish event resets cursor to start (ws.setTime(0)) so the amber cursor doesn't pin at the right edge after playback ends.\n\n**WaveformEditor** (edit page, always edit mode) — cut creation only. Custom pointer overlay (absolute inset-0, touch-none) handles both tap-to-seek and drag-to-create-region. Red draggable/resizable regions. Trim shortcut (pre-seeds start+end cuts). Split mode (mark a time position, confirm → creates TWO new clips: Part A (0→splitMs) and Part B (splitMs→end); original clip left completely unchanged as a reference; navigates to session page showing all three clips). Submit → new version in Postgres, router.back().\n\n**Non-destructive split**: original clip is never mutated. Part A and Part B are independent new clips, each with their own v1 containing appropriate cutMarks. All three clips share the same Drive source file — no audio is copied. Split provenance is tracked via a sourceClipId self-relation in Postgres. The editor shows a scissors icon + description on 'Start from' version buttons for split-origin versions. The original clip's editor renders a 'Split into' provenance card listing Part A and Part B.\n\n**ClipDetailClient** — owns selectedVersionId state. Feeds activeCuts to WaveformPlayer. Feeds activeVersionId to CollaborationSection for version-aware voting. Version list as large tappable cards. Clicking active version keeps it selected (no deselect/null state). Edit link to /edit page.\n\nVersions are metadata only (cutMarks JSON) pointing at the same source audio. Source audio never mutated. Linear chain. localStorage safety net for in-progress edits.",
  "successCriteria": "- Version list shows full chain: v number, description, result duration, cut count\n- Selecting a version in the detail page updates the waveform player to skip that version's cuts\n- Click-to-seek works on the waveform — clicking anywhere moves the playhead, play starts from that position\n- Stamp chips in the player are tappable and seek to that timestamp\n- After playback ends, cursor resets to start (no amber pin at right edge)\n- Submit button is always active — description is optional\n- Any member can create a version from any existing version\n- Trim shortcut pre-seeds start+end cut marks\n- Split: marks a time position → creates Part A (0→split) and Part B (split→end) as two independent new clips; original clip unchanged → navigates to session page showing all three clips\n- Original clip's editor shows a 'Split into [Part A], [Part B]' provenance card\n- 'Start from' version buttons show scissors icon + description text for split-origin versions\n- Drag on the edit waveform creates a cut region\n- Freeze locks the version and triggers render (see feat-render)\n- In-progress edits survive browser crash via localStorage draft\n- Draft banner shown on editor reopen with Resume / Dismiss",
  "implementation": "1. components/clips/WaveformPlayer.tsx: pure player component. Props: clipId, sourceDurationMs, activeCuts[], stamps[]. Regions rendered with pointerEvents:none (display only, don't intercept seek clicks). timeupdate handler: position tracking (currentTimeMsRef + setCurrentTimeMs) runs unconditionally — isPlaying() guard wraps only the cut-skip section so currentTimeMs updates correctly even while paused (needed for re-seek before play). finish event: ws.setTime(0) resets cursor to start. Stamp chips are <button> elements that call ws.setTime(). No enableDragSelection.\n2. components/clips/WaveformEditor.tsx: edit-only component. Props: clipId, sourceDurationMs, frozenVersionId, initialVersions, stamps, sessionHref, splitInto[]. Custom pointer overlay (onPointerDown/Move/Up + setPointerCapture, touch-action:none) handles tap-to-seek + drag-to-create. Trim/Clear all/Split tool buttons. Split navigates to sessionHref after success. Auto-saves draft on every cut change. router.back() after version submit. 'Start from' version buttons show scissors icon + description when version.description is set. 'Split into' provenance card rendered when splitInto.length > 0 — shows child clip names as pill badges.\n3. components/clips/ClipDetailClient.tsx: coordinator. Owns selectedVersionId state (auto-selects latest, no deselect). Passes activeCuts to WaveformPlayer. Passes activeVersionId to CollaborationSection. Actions section hidden when !canEdit && !frozen.\n4. app/(auth)/.../clips/[clipId]/page.tsx: server component — loads clip, versions, stamps, votes, comments, membership. Renders ClipDetailClient.\n5. app/(auth)/.../clips/[clipId]/edit/page.tsx: server component — loads clip, versions, stamps, splitInto (select id, name). Auth guard (MEMBER redirect + frozen redirect). Maps splitInto and passes to WaveformEditor.\n6. app/api/clips/[clipId]/versions/route.ts: POST creates a new version.\n7. app/api/clips/[clipId]/split/route.ts: POST { splitMs } — creates TWO independent new clips: Part A (cutMarks: [{startMs:splitMs, endMs:sourceDurationMs}], plays 0→split) and Part B (cutMarks: [{startMs:0, endMs:splitMs}], plays split→end). Both have sourceClipId pointing to the original. Original clip is completely unchanged. Returns { clipA: {id, name}, clipB: {id, name} }.\n8. lib/utils.ts: formatPosition(ms) → H:MM:SS always. formatDuration(ms) → M:SS. calcResultDuration(sourceDurationMs, cuts) → virtual duration after removing cut segments.\n9. prisma/schema.prisma: Clip.sourceClipId String? (nullable) + SplitFrom self-relation (sourceClip / splitInto) with onDelete: SetNull — deleting a child clip does not cascade to the original.",
  "decisions": [
    {
      "decision": "WaveformPlayer and WaveformEditor are separate components",
      "rationale": "A single component that handles both playback and editing requires mode-switching logic, conditional overlays, and competing event handlers. Separate components have clear single responsibilities: Player = listen, Editor = cut. The edit page is always in edit mode; no toggle needed. The detail page never needs editing. Each component can be reasoned about and tested independently.",
      "recommendation": "WaveformPlayer lives on clip detail and share pages. WaveformEditor lives exclusively on the /edit page. ClipDetailClient orchestrates state shared between Player and other sections (version pills, collaboration voting).",
      "date": "2026-04-11"
    },
    {
      "decision": "Custom pointer overlay for drag-to-create in editor (no enableDragSelection)",
      "rationale": "WaveSurfer's enableDragSelection registers a permanent pointer overlay that consumes all pointer events — including click-to-seek. Once called, seeking by clicking is broken. A custom absolute-positioned overlay with onPointerDown/Move/Up + setPointerCapture handles both tap-to-seek (< 8px movement) and drag-to-create (>= 8px) without blocking WaveSurfer's own event handling on other elements.",
      "recommendation": "In WaveformEditor: absolute inset-0 div with touch-action:none as overlay. tap → seekTo(); drag → addRegion(). No enableDragSelection() call anywhere.",
      "date": "2026-04-11"
    },
    {
      "decision": "pointerEvents:none on display-only regions in WaveformPlayer",
      "rationale": "WaveSurfer RegionsPlugin renders region overlays as absolutely positioned divs above the waveform canvas in z-index. Without pointerEvents:none, clicking on a cut region (the blue overlay) is swallowed by the div before reaching WaveSurfer's click-to-seek handler. The cursor moves but the playhead doesn't update.",
      "recommendation": "After addRegion() in WaveformPlayer, set region.element.style.pointerEvents = 'none'. This makes all regions display-only and lets all clicks pass through to WaveSurfer's seek handler.",
      "date": "2026-04-11"
    },
    {
      "decision": "Non-destructive split — original clip unchanged, two new independent clips created",
      "rationale": "Mutating the original clip on split (adding a new version) gave the wrong mental model: the original appeared consumed. The correct model is: Song X is a permanent reference; Part A and Part B are two new songs derived from it. All three exist independently and can be edited separately. Source audio is shared (not copied) since both children reference the same driveFileId and apply their cuts virtually.",
      "recommendation": "Split API creates two new Clip rows with sourceClipId pointing to the original. Original clip is not touched. All three clips appear in the session clip list after the split.",
      "date": "2026-04-11"
    },
    {
      "decision": "Split navigates to session page, not back to clip detail",
      "rationale": "After a split, three clips exist (original + Part A + Part B). router.back() would return to the original clip's detail — neither Part A nor Part B would be visible. router.push(sessionHref) lands on the session clip list where all three are visible and accessible.",
      "recommendation": "WaveformEditor accepts sessionHref prop. After successful split API call: router.push(sessionHref).",
      "date": "2026-04-11"
    },
    {
      "decision": "No version deselect — clicking active version keeps it selected",
      "rationale": "Deselecting a version set selectedVersionId to null, which caused the player to show 'no cuts' (raw audio). This 'null = raw audio' state had no UI label and was visually identical to a version with no cuts. Confusing. Clicking the active version now keeps it selected — the player always reflects a known version.",
      "recommendation": "onChange={() => setSelectedVersionId(v.id)} — no toggle. To hear raw audio, select v1 (which has no cuts unless the clip was split).",
      "date": "2026-04-11"
    },
    {
      "decision": "Versions are metadata only — not audio files",
      "rationale": "Audio duplication per version is prohibitively expensive (43MB+ per clip). Versions are cut instructions (cutMarks JSON). Source audio stored once in Drive. Real audio file produced only on freeze.",
      "recommendation": "Postgres clip_versions: {cutMarks: [{startMs, endMs}], resultDurationMs, description, fromVersionId, createdBy}.",
      "date": "2026-04-10"
    },
    {
      "decision": "localStorage for in-progress edit safety",
      "rationale": "Full offline sync adds complexity. The real risk is a browser crash mid-edit. localStorage persists cut marks across crashes. Key: odio:draft:{clipId}. Cleared on successful submit.",
      "recommendation": "WaveformEditor writes cutMarks to localStorage on every change. Draft banner shown on reopen. Resume loads draft; Dismiss clears it.",
      "date": "2026-04-10"
    }
  ],
  "knownLimitations": [
    "Split shares source audio between Part A and Part B until both are frozen — both clips point to the same Drive file and apply different cuts virtually",
    "Editor overlay blocks region resize handles on existing cuts — to adjust a cut, remove it and redraw",
    "If two members edit the same version simultaneously they both produce the next version number — band sees two versions at the same level and must choose",
    "Submit requires internet connection — cuts safe in localStorage but cannot be written to Postgres offline"
  ],
  "componentFile": "components/clips/WaveformEditor.tsx",
  "userGuide": "From a clip's detail page, select a version in the Versions section to hear that version's cuts applied. Tapping stamp chips (🔥 ⭐ etc.) in the player seeks to that moment. Tap 'Edit cuts' to open the full-screen editor. In the editor: drag on the waveform to create a red cut region; tap Trim to pre-seed start/end cuts; tap Clear all to reset; tap Split to non-destructively divide the song at a chosen time position — this creates Part A (0 to the split) and Part B (split to end) as two new independent clips while leaving the original untouched. All three appear in the session. The original clip's editor shows a 'Split into' card listing Part A and Part B. Tap 'Start from' pills to load a previous version's cuts as a starting point. Tap 'Submit edit' to save as a new version — description is optional.",
  "statusHistory": [
    {
      "from": "draft",
      "to": "active",
      "date": "2026-04-10"
    },
    {
      "from": "active",
      "to": "frozen",
      "date": "2026-04-10"
    },
    {
      "from": "frozen",
      "to": "active",
      "date": "2026-04-11",
      "reason": "Major architectural redesign: WaveformPlayer + WaveformEditor split, ClipDetailClient coordinator, version deselect fix, split navigation, click-to-seek fix"
    }
  ],
  "revisions": [
    {
      "date": "2026-04-11",
      "author": "marc+claude",
      "fields_changed": [
        "title",
        "analysis",
        "implementation",
        "decisions",
        "knownLimitations",
        "successCriteria",
        "userGuide",
        "tags"
      ],
      "reason": "Separated WaveformPlayer (pure play) from WaveformEditor (pure edit). ClipDetailClient owns selectedVersionId. Click-to-seek fixed via pointerEvents:none on regions. Split navigates to session page. Stamp chips are tappable. Version deselect removed."
    },
    {
      "date": "2026-04-11",
      "author": "marc+claude",
      "fields_changed": [
        "analysis",
        "implementation",
        "decisions",
        "successCriteria",
        "userGuide"
      ],
      "reason": "Non-destructive split: original clip unchanged, creates two independent new clips (Part A + Part B) with sourceClipId provenance. Scissors icon + description on split-origin version buttons. 'Split into' provenance card on original clip editor. WaveformPlayer finish event resets cursor to start. timeupdate guard scope fix (position tracking unconditional, isPlaying gate wraps only cut-skip section). Schema: sourceClipId self-relation added to Clip."
    }
  ]
}
